<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xmniao.xmn.core.xmer.dao.FlowInfoDao">
	<!-- 查询寻蜜客好友的id -->
	<select id="queryFriendShipByUid" parameterType="int" resultType="int">
		SELECT DISTINCT uid FROM b_xmer where parentid = #{parentid}
	</select>
	
</mapper>