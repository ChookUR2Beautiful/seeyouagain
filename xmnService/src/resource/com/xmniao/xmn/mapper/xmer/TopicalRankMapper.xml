<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xmniao.xmn.core.xmer.dao.TopicalRankDao">
	
	<!-- 根据用户uid查询用户的销售数量-->
	<select id="queryXmerByUid" resultType="int" parameterType="int">
		SELECT sold_nums FROM b_xmer WHERE uid = #{uid};
	</select>
	
</mapper>